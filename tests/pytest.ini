[pytest]
DJANGO_SETTINGS_MODULE = config.settings
pythonpath = app/backend
testpaths = tests/backend
markers =
    live: requires live network access to ebanglalibrary.com (deselect with -m "not live")
